NEXT_PUBLIC_BACKEND_API_URL="http://localhost:8000/api"
# Replace localhost:8000 with your actual backend URL if different,
# especially when running backend in Docker. If backend is also in Docker Compose
# on the same network, you might use the service name e.g., http://backend:8000/api
